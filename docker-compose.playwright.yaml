#ddev-generated
services:
  playwright:
    container_name: ddev-${DDEV_SITENAME}-playwright
    hostname: ${DDEV_SITENAME}-playwright
    # Playwright version = the Docker image tag. Change it by setting
    # PLAYWRIGHT_IMAGE_TAG in .ddev/.env (e.g. `ddev dotenv set .ddev/.env
    # --playwright-image-tag v1.57.0-noble`), then `ddev restart`. The default
    # below is used when it is unset.
    image: mcr.microsoft.com/playwright:${PLAYWRIGHT_IMAGE_TAG:-v1.56.1-noble}
    command: ["tail", "-f", "/dev/null"]
    restart: "no"
    working_dir: /var/www/html
    environment:
      # Browsers ship preinstalled in the official image; point Playwright at them
      # so nothing is ever downloaded into the project.
      - PLAYWRIGHT_BROWSERS_PATH=/ms-playwright
      - VIRTUAL_HOST=$DDEV_HOSTNAME
      - DDEV_SITENAME=${DDEV_SITENAME}
      # Native Playwright UI (8077) + HTML report server (9323) via the DDEV router.
      # UI reachable at https://<site>.ddev.site:8078, report at :9324.
      - HTTP_EXPOSE=8077:8077,9323:9323
      - HTTPS_EXPOSE=8078:8077,9324:9323
    # These labels ensure this service is discoverable by DDEV.
    labels:
      com.ddev.site-name: ${DDEV_SITENAME}
      com.ddev.approot: ${DDEV_APPROOT}
    external_links:
      - "ddev-router:${DDEV_SITENAME}.${DDEV_TLD}"
    volumes:
      # Project root: the app is reachable at http://web from this container, and
      # your Playwright install (package.json / node_modules / tests) lives here,
      # managed entirely by you.
      - "../:/var/www/html"
      - "ddev-global-cache:/mnt/ddev-global-cache"
